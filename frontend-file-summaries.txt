{
  "frontend/src/index.css": "Global styles with GreatAndhra.com-inspired light theme using OKLCH color tokens — white/light-grey background, red accent, Roboto/Roboto Condensed fonts, and utility classes for news-specific UI elements including ticker animation, category badges, section headers, and nav tabs.",
  "frontend/tailwind.config.js": "Updated Tailwind config with light-theme news color tokens, Roboto/Roboto Condensed font families, sharp border radii, and news-specific shadows matching the GreatAndhra.com aesthetic.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — all query hooks now merge actorFetching into isLoading for a unified loading state, all mutation hooks guard against unready actor with a clear error message, and added useGetCallerUserProfile, useIsCallerAdmin, useGetCallerUserRole, useSaveCallerUserProfile, and useAssignCallerUserRole hooks.",
  "frontend/src/components/Header.tsx": "Three-row sticky header with breaking news ticker, logo/date row with admin links (\"Movie Reviews\" and \"Add Article\") on both desktop and mobile, and category tab navigation.",
  "frontend/src/components/Footer.tsx": "Updated footer with white/light background, red accent section headers, logo image, quick links, categories, copyright, and caffeine.ai attribution with UTM tracking.",
  "frontend/src/components/ArticleCard.tsx": "Article card component with four variants (featured, newspaper, default, compact); all image references updated from `article.imageUrl` to `article.imageData` to match the current backend `News` type.",
  "frontend/src/pages/HomePage.tsx": "Homepage with a leaderboard AdBanner (728×90) inserted between the hero section and the main content grid.",
  "frontend/src/pages/PoliticalNewsPage.tsx": "Redesigned political news listing page with two-column layout — main column shows articles as horizontal list items (ArticleListItem), sidebar shows top 5 stories via TopStoriesSidebar, with load-more pagination.",
  "frontend/src/pages/MovieNewsPage.tsx": "Redesigned movie news listing page with two-column layout — main column shows articles as horizontal list items (ArticleListItem), sidebar shows top 5 stories via TopStoriesSidebar, with load-more pagination.",
  "frontend/src/pages/ArticleDetailPage.tsx": "Full article detail page with reviews section, related articles, and admin delete controls; updated both `article.imageUrl` references to `article.imageData` to fix TypeScript errors.",
  "frontend/src/App.tsx": "Main app with TanStack Router — root layout wrapping Header/Footer with Outlet, now with ActorLoadingGuard wrapping the Outlet so no page content renders until the backend actor is ready.",
  "frontend/src/components/BreakingNewsTicker.tsx": "New component that fetches the 10 most recent articles and displays them in a horizontally auto-scrolling ticker with a dark-red \"Breaking\" badge on the left and clickable headline links.",
  "frontend/src/components/ArticleListItem.tsx": "Horizontal list item component for article listings; updated image source reference from `article.imageUrl` to `article.imageData` to match the backend interface.",
  "frontend/src/components/TopStoriesSidebar.tsx": "Top stories sidebar component now wrapped in a `div` with a rectangle AdBanner (300×250) placed below the numbered stories list.",
  "frontend/src/pages/AdminPage.tsx": "Admin content management page with a \"Add New Article\" form; the image field has been replaced with a file upload button that opens the device's native file picker (accepting JPEG, PNG, GIF, WebP), converts the selected image to a base64 data URL via FileReader, and displays a thumbnail preview with Change/Remove controls. The article list now correctly references `article.imageData` instead of `article.imageUrl`.",
  "frontend/src/pages/AdminReviewsPage.tsx": "New admin page at /admin/reviews that lists all reader reviews with reviewer name, star rating, article ID, review text, and date; each review has a delete button with confirmation dialog using useDeleteReview mutation.",
  "frontend/index.html": "HTML entry point with the Google AdSense async script tag in the `<head>` section, now using the correct publisher ID `ca-pub-2965680356233756`.",
  "frontend/src/components/AdBanner.tsx": "New reusable Google AdSense ad unit component accepting `adSlot`, `adFormat` ('horizontal' | 'rectangle'), and optional `className` props; calls `adsbygoogle.push({})` on mount with graceful error handling and an \"Advertisement\" compliance label.",
  "frontend/src/components/ActorLoadingGuard.tsx": "Guards app content behind a successful actor initialization — shows a spinner while connecting, an error card with retry button on failure (when actor is null), and renders children when the actor is ready.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor using createActor, verifies connectivity via isConnected(), and exposes actor, isFetching, and error states via React Query with retry logic."
}